
# ─── Supabase (public) ─────────────────
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.xxxxxx

# ─── Google OAuth (frontend) ──────────
# Use the SAME client for dev & prod, or a separate one per env.
VITE_GOOGLE_CLIENT_ID=123456789012-abcdefg1234567890apps.googleusercontent.com
# Must match one of the redirect URIs configured in that client.
VITE_GOOGLE_REDIRECT_URI=https://your-project.supabase.co/functions/v1/google-oauth-complete

# (Optional) If you use a different port in dev, add that origin
# to “Authorized JavaScript origins” in Google Cloud.
# Example: http://localhost:3001

# ─── Backend-only (Edge Function) ─────
# These are NOT prefixed with VITE_ and must be set as project
# environment variables in Supabase, not in the browser bundle.

# GOOGLE_CLIENT_ID      = same as above
# GOOGLE_CLIENT_SECRET  = <copy from Cloud Console>
# GOOGLE_REDIRECT_URI   = same as above
# SERVICE_ROLE_KEY      = <Supabase service-role key>

# ──────────────────────────────────────